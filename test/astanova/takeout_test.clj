(ns astanova.takeout-test
  (:require [clojure.test :refer [deftest is testing]]
            [astanova.takeout :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
